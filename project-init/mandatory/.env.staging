# Staging Environment Configuration
# This is a TEMPLATE - set actual values in Railway UI
# Never commit real secrets to Git!

NODE_ENV=staging

# Railway provides PORT automatically
# PORT is set by Railway

# Database (set in Railway UI)
# Example: DATABASE_URL=${{Railway.POSTGRES_URL}}
DATABASE_URL=

# API Configuration
API_URL=https://[project-name]-staging.up.railway.app

# Third-party APIs (set real keys in Railway UI)
# Example values below - replace in Railway
OPENAI_API_KEY=
STRIPE_SECRET_KEY=
SENDGRID_API_KEY=

# Feature Flags
ENABLE_DEBUG_LOGGING=true
ENABLE_EXPERIMENTAL_FEATURES=true

# CORS
CORS_ORIGIN=https://[project-name]-staging.up.railway.app

# Session
SESSION_SECRET=  # Generate strong secret in Railway UI

# Generated by keithstart
# Replace [project-name] with your actual project name
# Set real values in Railway dashboard: Settings → Variables
